[project]
name = "gpt2-hello-world"
version = "0.1.0"
description = "A beginner-friendly GPT-2 hello world example."
readme = "README.md"
requires-python = ">=3.11,<3.12"
dependencies = [
    "torch",
    "transformers",
]

[tool.uv]
package = false
